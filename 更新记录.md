# 更新记录

本文件记录「轻切」从立项到 Electron 桌面端实装过程中的全部改动（按阶段划分，最新在前）。

应用内「更新记录」页（`#/changelog`）读取 `src/renderer/src/lib/changelog.ts`，与本文保持同一份口径。

## v0.8.4 — 距离测量改为 Figma 式边缘间距（2026-09-26）

- 悬停测量语义重做：不再从选中图层中心引线，改量选中包围盒与悬停图层「边到边」的水平/垂直间隙（ΔX、ΔY），测量线两端画垂直刻度、中点画数值标签（11px 等宽字，accent-2 底）
- 某一轴重叠时测量线落在重叠区中线；斜向错位时两线成 L 形连接最近角；双向重叠只显示两个高亮虚线框
- 选中包围盒与悬停图层同时以 accent 虚线框标出，跟随主题色；v0.8.3 的中心距语义废弃

## v0.8.3 — 画布距离测量与滑杆主题化（2026-09-26）

- 距离测量：`CanvasView` 选择工具下已选中图层时，鼠标悬停其它图层即绘制「选中包围盒中心 → 该图层边框最近点」虚线、该图层高亮框、中心锚点与中点距离标签（v0.8.4 起改为边缘间距）
- 质量滑杆由原生 `input[type=range]` 换为 `components/Slider.tsx`（div 自绘，轨道 / 填充 / 手柄全走 `--line-2 / --accent / --bg`），属性面板、批量导出弹层、设置页三处统一，跟随 6 套主题与自定义配色
- 主按钮去渐变：`.btn-primary` 与批量导出 `.batch-mini.go` 由 `--accent-grad` 改纯色 `--accent`
- 属性面板 `.layer-preview` 高度由固定 140px 改为 `min-height` + 图片按比例自适应，长图不再被裁
- 构建修复：v0.8.2 漏提交 `lib/exportPrefs.ts`（SettingsPage 已引用），补交后远端 main 恢复可构建

## v0.8.2 — 设置页补全：导出 / 存储 / 导航（2026-09-26）· commit `fb1b507`

- 导出设置区：默认格式（PNG / JPG / WebP）、倍数（@1x/@2x/@3x 可多选）、非 PNG 质量滑杆，持久化 `qingqie.export-prefs`；详情页批量导出参数以它初始化并双向同步，加载时校验倍数合法值
- 存储与缓存区：主进程新增 `app:data-stats`（递归统计 userData 总容量 / 缩略图容量）、`app:open-data-dir`、`app:clear-thumb-cache`；页内显示占用容量、一键打开数据目录、确认弹窗后清空缩略图缓存并 toast 释放量（列表回退占位图，重新导入再生成）
- 导航优化：选中菜单项左侧 accent 渐变指示条；搜索支持关键词别名（如「缓存」「目录」「缩放」「配色」）命中，命中项下显示灰色匹配词
- 删除设置页「外观」区（亮/暗快切与「主题」重复），进设置页默认落在「主题」；关于页数据卡并入底部一行指引「数据与缓存管理见 设置 → 通用 → 存储与缓存」

## v0.8.1 — 新版检查提醒与品牌 Logo 主题化（2026-09-26）· commit `9d6db00`

- 更新检查（无后端方案）：主进程 IPC `app:check-update` 请求 GitHub API `repos/dhk333/QinQIe/releases/latest`（8s 超时），tag 去 `v` 后与 `app.getVersion()` 三段数字比较；404 / 断网 / 超时 / 格式异常一律静默返回 null
- 顶栏「轻切」旁显示「新版 vX」胶囊（主题色跟随），点击经 `setWindowOpenHandler → shell.openExternal` 打开 Release 下载页；× 按版本号写入 localStorage 忽略，同版本不再提示
- 品牌 Logo 全局替换为 concept-D「裁切刀 + 切片虚线框」（`components/AppLogo.tsx`），`--logo-blade / --logo-ring / --logo-frame / --logo-sheet` 四变量进全部 6 套主题块，切主题即时换色；`build/icon.svg` 同步重制并再生 png / ico（exe 图标为静态，需重新打包生效）
- 加载遮罩打磨：Logo 描边动画 64 → 46px、文案「加载中，马上就好…」、间距 +8px；`.theme-toggle:hover` 去除 `rotate(15deg)`
- `package.json` 版本 0.1.0 → 0.8.1（更新比对基准）；发布 GitHub Release `v0.8.1` 作为更新通道起点

## v0.8 — 快捷键体系与设置页（主题 / 字体 / 更新日志）（2026-09-26）· commit `35a6eb1`

- 共享键位注册表 `src/shared/keymap.ts`：25 条命令单一来源，键盘分发、帮助面板、右键菜单提示与原生菜单同源；用户改绑覆盖存本机（override 优先匹配、冲突检测、单条 / 全部恢复默认）
- Electron 原生应用菜单（轻切 / 编辑 / 视图 / 图层 / 导出），`registerAccelerator:false` 与页面键位同源，点击经 IPC `menu:exec` 分发到当前页；`?` 打开全局快捷键一览弹窗，`Ctrl+\` 可改绑
- 设置独立页 `#/settings`：顶栏齿轮进入，左菜单右内容（外观 / 主题 / 字体 / 快捷键设置 / 更新日志 / 关于），带搜索过滤，菜单切换统一渐入渐出
- 主题：6 套全局配色（晴空 / 墨夜 / 青瓷 / 暖沙 / 绛紫 / 松墨）+ 自定义主题——背景 / 面板 / 文字 / 主色 4 基色自动推导整套 token，可选亮 / 暗基准，实时预览、持久化、可删除回落
- 字体大小：界面整体等比缩放三档（小 = 默认 / 中 ×1.1 / 大 ×1.25，html zoom），首帧前应用避免启动跳变
- 「更新记录」更名「更新日志」并独立成分组；排版重做为左日期 / 版本 + 右标题 / 要点的两栏时间线，去掉逐条弹跳动画；「关于轻切」重排（hero + 版本号 + 核心能力卡 + 数据与隐私键值卡）
- 顶栏控件圆角统一 8px：`.g-topbar .btn`、`.tb-search`、`.theme-toggle`；`.btn` 全局由胶囊改为 8px 圆角矩形
- 设置区宽度策略：`.set-sec-wide` 限宽 980px，主题网格随窗口铺满

## v0.7 — 导出引擎重写与切片/图层工作流（2026-09-26）· commit `17b8b10`

- 导出链路重写：canvas → Blob → 字节流 IPC 直写落盘，替换 base64 dataURL 中转；批量导出改为「选一次目录、全部直接写文件」，不再逐个弹保存框
- 批量导出参数弹层：格式（PNG / JPG / WebP）、倍数可叠加（@1x / @2x / @3x）、非 PNG 时质量滑杆，按钮实时显示「导出 N 个文件」
- 导出进行中显示进度浮层（done / total）并可取消；同名文件自动加 `（n）` 后缀；写盘前对文件名做 `basename` 归一，防目录穿越
- 切片持久化：随文档写入 projects.json，重进页面自动恢复、序号接着排；编辑静置 500ms 提交
- 切片撤销 / 重做：Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y（快照栈，上限 80 步），撤销与重做同步落盘
- 切片支持重命名与单切片覆盖格式 / 倍数（默认「跟随批量设置」）；画布切片角标显示「编号·名称」
- 图层多选：图层树与画布支持 Ctrl 点选增删、Shift 区间选择，多选框线在画布同步高亮
- 图层右键菜单：导出所选 N 个图层 / 从图层创建切片（组取整组外接框、名称跟随图层）/ 隔离显示 / 显示全部图层

## v0.6 — 顶栏与详情页交互打磨（2026-09-25）· commit `643c287`

- 新增「更新记录」页：`lib/changelog.ts`（数据）+ `pages/ChangelogPage.tsx`（列表）+ `#/changelog` 路由，顶栏图标 + 文字入口，返回回到进入前的页面
- 修复详情页刷新/热更新后整页空白（暗色下表现为画布全黑）：projectId / psdId 改由路由 `#/detail/<项目>/<画板>` 承载，不再只存在内存 state
- 详情页加载态收敛为一个：logo 描边动画 + 「正在解析 PSD…」，覆盖到位图解码阶段；移除「正在解码图层…」胶囊
- 左右侧边栏默认展开，去掉「先收起、解析完再弹出」的跳动
- 顶栏控件统一 32px 高（按钮 / 返回 / 主题切换 / 面包屑 / 搜索框）；面包屑垂直居中并去掉状态圆点；`.ver` 版本号胶囊移除
- 详情页面包屑 `.file-chip` 绝对定位，在「顶栏减去右侧窗口按钮（`--win-ctl-w: 132px`）」的可用宽度里水平居中，不随左右内容宽度漂移；过长文件名省略号截断
- 属性面板 CSS 代码块支持鼠标框选复制（全局 `user-select: none` 的例外）
- logo 尺寸调整：顶栏 31×31，详情页加载动画放大到 63×69
- 去掉顶栏页面标题 `.page-title`（我的项目 / 更新记录），以及图层面板里与 `panel-head` 重复的「图层 + 数量」小标题
- 图层数量改为递归统计：此前只数顶层节点（关于我们3.psd 显示 16，实际 356 行 / 247 个叶子图层），批量条「共 N 个可见图层」同样修正（229）

## v0.5 — 自研合成器，逐像素对齐 Photoshop（2026-09-24）· commit `296e702`

- 新增 `lib/compositor.ts`（浏览器与 Node 共用、零 DOM 依赖）：填充不透明度与图层不透明度分离、图层/组蒙版、组离屏合成、剪贴链、图层样式（投影 / 内投影 / 描边 / 颜色叠加 / 渐变叠加 / 内外发光）
- 预览、单图层导出、切片导出统一走同一份实现，保证「所见 = 所导出 = 回归基线」
- 按 PS 语义实现光照角约定与三遍可分离盒模糊；蒙版灰度按 ag-psd 的 RGB 明度还原
- `pnpm verify` 两道门禁：`verify:psd` 以 PSD 内嵌合成图为 oracle 做逐像素回归（判定 mean < 1 且差异占比 < 1%），`verify:app` 校验两阶段解析的图层 id 映射与单图层出图尺寸
- 画布缓存整篇合成结果，平移缩放只搬运位图
- 明确否掉「直接显示 PSD 内嵌合成图」的捷径：合成图缺少图层信息，无法满足单图层导出与选中命中

## v0.4 — 两阶段加载提速与解析容错（2026-09-24）· commit `d1f5254`

- 结构先行：先解析图层树与文档信息（约 50ms）让面板立即可用，位图随后补齐
- ag-psd v29 坏蒙版数据经 patch-package 补丁固化
- `@webtoon/psd` 兜底解析器修复，主解析器不支持的文件（ZIP 压缩等）仍可打开

## v0.3 — 品牌图标与打包配置（2026-09-24）· commit `445d23f`

- 顶栏 / 加载动画 / 原型统一换用四色 logo SVG
- `build/icon.svg` 按内容包围盒居中，新增 `gen-icon` / `gen-ico` 脚本产出 png 与多尺寸 ico
- `win.icon` 指向 `build/icon.ico`；`dist` 经 `dist-retry` 绕开 Defender 锁定 exe 导致的写入失败
- 新增 `README.md` 与 `更新记录.md`

## v0.2 — 桌面端实装（2026-09-24）· commit `16565ef`

### 架构与数据层（阶段 1）
- 新增 hash 路由（`lib/router.ts`）：`#/home` → `#/project/:id` → `#/detail`；详情页上下文（projectId/psdId）存 React state
- 新增 `lib/projects.ts` + 主进程 IPC：`projects.json` 持久化（PSD 只存路径）；`psd:pick/import/read`、`file:exists/read-dataurl/pathsForFiles`（webUtils 拖拽取路径）
- 主进程生成 PSD 缩略图（ag-psd 合成 → @napi-rs/canvas 缩放），按 路径+大小+mtime 哈希缓存；主解析失败自动走 @webtoon/psd 兜底

### 页面实装（阶段 2-4）
- `HomePage`：我的项目列表、新建/重命名/删除项目（自定义弹窗 + Toast）
- `ProjectPage`：横向画板流、上传 PSD、画板重命名/删除、分组 CRUD、拖拽归类、丢失文件检测（PsdThumb 组件）
- `DetailPage`：三栏工作台（图层树 / 画布 / 属性面板），面板可拖宽、收起
- 无边框窗口（frame:false）+ 自绘最小化/最大化/关闭（close hover #e81123），顶栏拖拽区 -webkit-app-region
- 修复「详情页空白」：路由不带项目上下文，改为 App state 记录 { projectId, psdId }

### PSD 解析与渲染
- 渲染管线抽到 `lib/psd.ts`：drawSiblings（剪贴蒙版链 + destination-in）、buildCompositeCanvas、renderLayerCanvas（图层/组统一出图，供预览与导出共用）
- 图层蒙版烘焙：ag-psd mask.canvas 的 RGB 明度 → alpha，defaultColor 补区
- 修复画布全空白：ag-psd v29 opacity 已归一化 0~1（此前误再除 255）
- **双解析器**：ag-psd 失败时 @webtoon/psd 兜底（支持 ZIP 压缩）；BlendMode 内部键映射表；default export 引入方式修正
- **文本图层**：内容取 `text.text`（此前误读 `content` 导致文本框为空）；字体名 `style.font.name`；fillColor 0~1/0~255 归一化为 HEX；字重按 fontWeight≥700 / 名称后缀（Bold/Heavy/Black/-B）推断
- **组的 bounds 修复**：ag-psd 组节点尺寸常为 0，改为子图层并集推算（groupBounds，兜底解析器同理）；修复 toNode/walk 中 children 重复 map 导致树节点 id 与 canvasMap 错位

### 画布与交互
- 移动工具点击选层：rect 候选倒序 + getImageData 像素 alpha 命中（容差约 2 屏幕像素），不再总选中背景层；点空白取消选中
- 图层树联动：外部选中自动展开祖先分组 + scrollIntoView；根元素 w-full flex-1 min-h-0（支持纵向滚动、宽度随面板拖拽）
- 取色器接通画布点击（onPickColor 此前从未被调用）
- 切片：Shift/Ctrl 多选、右上角红叉删除、拖动、8 控制柄缩放、X/Y/W/H 数值输入；batch-bar 切片/图层双模式 + 横向滚动（按钮不再被压扁）
- zoombar 工具高亮（.zb.active，修复原型移植遗漏）；缩放比例下拉菜单 25/50/75/100%

### 属性面板
- 文本区块：内容可复制（.text-box + .cp）、字号/颜色（色块+HEX 点击复制）/字重/字体
- CSS 区块输出 `font-size / font-family（去字重后缀）/ font-weight / color`，可一键复制；替换原「文本图层占位注释」
- 图层/组预览：棋盘底 + 组按当前显隐合成裁剪（renderLayerCanvas）
- 组导出修复：选中组可导出合并图（此前提示「无位图内容」）
- 组显隐修复：切换组眼睛传导到全部后代图层 id（渲染/导出只检查叶子）
- 面板滚动修复：纵向 flex 容器内组件去掉 shrink-0（会禁掉高度压缩导致无法滚动）
- 格式/倍数选项按钮 cursor:pointer

### 视觉与动效
- 详情页左右面板默认收起，解析完成自动展开：.panel width 0.28s + 内容 opacity 0.18s（不用 display:none/overflow:hidden，避免裁掉拖拽柄与吃不到动画）
- 「正在解析 PSD…」遮罩：层级提到 z-index 30（盖过 zoombar/拖拽柄），logo 四路径描边动画（pathLength=1 归一化，描绘→停顿→擦除 2s 循环，逐条错峰），白色 12px 文案
- 全局沿用原型规范：默认亮色、蓝色渐变主色、扁平轻投影

### 工程与质量
- `@napi-rs/canvas` 从 devDependencies 移入 dependencies（修复主进程原生模块打包损坏）
- 新增诊断脚本 `scripts/debug-text.mjs`（dump 文本图层原始 JSON）
- 每轮改动跑 `npm run typecheck`；ship 前 `npm run build` 全绿
- 本地 ship 流程（无远端）：main → feat 分支 → commit → squash 合并 → 删临时分支 → 清 stash

## v0.1 原型打磨（design/index.html）· commits `d6f7089`…`f0bed99`

- `d6f7089` chore: 项目基线（Electron + Vite + React + TS + Tailwind v4 脚手架，阶段一 MVP：打开 PSD/图层树/画布/取色/导出/复制 CSS）
- 修复「PSD 能导入但画布空白」：opacity 归一化、children 自底向上绘制顺序、剪贴蒙版与图层蒙版
- `a2b88fa` 高保真原型：亮/暗双主题（蓝湖风亮色）；主题色统一为蓝色渐变，切暗色不变
- `ac2f157` PS 风格切片工具（工具条后并入底部 zoombar 统一显示）
- `5a7f7ab` 三页信息架构：首页（我的项目）→ 项目列表（蓝湖式横向画板流）→ 详情；砍掉团队/协作概念
- 全局顶栏（左：返回+logo+版本；右：主题切换+窗口控制）；默认亮色；去掉「新建项目」虚线卡片与设计/原型 tabs
- `1441774` 画板重命名 + 分组管理（新增/重命名/删除分组、拖拽归类）；空状态改「错位卡片插画」
- `39c6d73` 自定义弹窗（prompt/confirm/danger）+ Toast 三态轻提示，替换全部 window.prompt/confirm
- `f0bed99` 动效系统：页面切换/弹窗/菜单过渡 + 列表交错入场（后应要求放慢）；确立「后台页面不丢失内容」安全模式（弃声明式 fill-mode 动画，JS 触发类名 + visibilityState 跳过）
- 细节定稿：`.btn { padding: 6px 15px; font-size: 12px }`、悬浮条阴影 `0 10px 30px rgba(0,0,0,.05)`、多处去 box-shadow、面板拖拽 chevron（rzChev）、项目下拉切换（.pname/.proj-menu）

## 已知待办

- 阶段 6：electron-builder NSIS 安装包 + 打包回归（应用图标已就绪，见 v0.3）
- 主题偏好持久化（记住用户上次切换）
- 切片选中与图层树进一步联动、文本多段混排样式细分
- `凌烟阁index.psd` 未纳入保真回归集：与内嵌合成图差异占比 1.143%，略高于 1% 阈值（mean 0.297/255）
